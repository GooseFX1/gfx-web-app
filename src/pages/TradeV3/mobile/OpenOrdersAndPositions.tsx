import React, { ReactElement } from 'react'
import { HistoryPanel } from '../HistoryPanel'

const OpenOrdersAndPositions = (): ReactElement => (
  <div>
    <HistoryPanel />
  </div>
)

export default OpenOrdersAndPositions
